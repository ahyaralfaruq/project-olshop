import { BurgerMenu, CartIcon, MessageIcon, NotificationIcon } from './navbar-icon'

const Navbar = () => {
    return (
        <div>
            <CartIcon />
            <MessageIcon />
            <NotificationIcon />
            <BurgerMenu />
        </div>
    )
}

export default Navbar
